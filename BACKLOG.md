# Backlog

Active and future work is tracked with stable `PS-###` work-item IDs. Status, numbering, relationship, and cross-repository reference rules are defined in `docs/repository-governance.md`.

## Repository Governance

| ID | Status | Target | Owner | Relationship | Related Work Item(s) | Work Item | Rationale / Notes |
|---|---|---|---|---|---|---|---|
| `PS-001` | Completed | Repository | Jeff Skone |  |  | Establish initial repository framework and governance | Established the public repository model, lean initial hierarchy, per-script documentation standard, component-scoped version/tag policy, compatibility and encoding defaults, backlog governance, Git/GitHub policy, and MIT licensing baseline. The framework commit intentionally receives no tag or GitHub Release. |
| `PS-002` | Completed | Repository | Jeff Skone |  |  | Set and verify initial GitHub repository configuration | Verified 2026-08-31: repository is `jefsko/powershell`, visibility is public, default branch is `main`, the approved description is exact, Website is blank, topics are `automation`, `powershell`, `powershell-scripts`, `scripting`, `system-administration`, `utilities`, and `windows`, and the About home-page sections are Releases enabled, Deployments disabled, and Packages disabled. Post-push verification confirmed the committed framework tree, intended commit identity, MIT license detection, and absence of unintended tags or GitHub Releases. |
| `PS-003` | Deferred | Repository | Jeff Skone |  |  | Evaluate script categorization and directory taxonomy | Do not introduce categories before the script inventory demonstrates a real need. Revisit when the number or diversity of scripts makes a flatter `scripts/<script-slug>/` structure materially harder to navigate. If categories are adopted, preserve clear component identity and avoid unnecessary path churn. |
| `PS-004` | Deferred | Repository | Jeff Skone | Related to | `PS-002` | Review optional GitHub repository features | Current observed state: Issues enabled, Projects enabled, Wiki enabled, and Discussions disabled. Revisit whether these defaults remain useful once the repository has substantive components. In particular, consider retaining Issues for public bug/question intake while disabling Projects and Wiki if they remain redundant with repository-owned backlog and documentation. Document any final configuration decision. |
| `PS-005` | Deferred | Repository | Jeff Skone | Related to | `PS-002` | Evaluate branch protection or repository ruleset | `main` is currently unprotected and no repository ruleset is configured. Revisit whether a minimal ruleset should protect against accidental force-push or branch deletion without imposing an unnecessary pull-request-only workflow. Add protection only when its practical value outweighs added workflow complexity. |
| `PS-006` | Open | Scripts | Jeff Skone | Related to | `PS-003`, `PS-007` | Create SHA-256 sidecar generation and validation scripts | Evaluate, plan, and develop the proposed SHA-256 file-integrity utilities further before treating the current drafts as final repository components. Scope includes `New-FolderSha256.ps1` to generate root-level `.sha256` sidecars for non-checksum files, `Test-FolderSha256.ps1` to validate file/sidecar pairs and clearly report complete success, partial matches, mismatches, missing files, missing sidecars, and invalid checksum files, and `PowerShell-SHA256-Sidecars-and-Validation-Addendum.md` as the current supporting design/usage documentation. Preserve useful source comments and comment-based help, generic non-sensitive examples, and the terminology distinction between SHA-256 as the algorithm, the SHA-256 hash as the computed value, and checksum/sidecar files as stored verification artifacts. Explicitly discuss and define the term `sidecar`, including why it is appropriate for a companion `.sha256` file, whether a more familiar term should be preferred in user-facing documentation, and how `sidecar`, `checksum file`, and `hash file` should be used consistently. During implementation, determine whether the two scripts should be one cohesive component or separate related components; confirm final names, directory structure, README/documentation placement, PowerShell 5.1 and PowerShell 7+ compatibility, sidecar format and overwrite safeguards, console output and exit-code behavior, exclusions, validation rules, testing strategy, and whether any proposed enhancements are warranted. In coordination with `PS-003`, evaluate whether this component and future related utilities justify a category subdirectory rather than the current flat `scripts/<script-slug>/` model. Consider candidate category names such as `checksums`, `file-integrity`, `hashing`, or another durable term; also explicitly consider nested category/subcategory structures such as `scripts/file-integrity/checksums/<component>/` when the inventory becomes large enough to justify that depth. Define the intended scope of each category and subcategory before adopting it, keep hierarchy as shallow as practical, and avoid introducing taxonomy for a single component unless it provides clear long-term value. The supplied scripts and addendum are starting artifacts and may be revised substantially as evaluation and development proceed. |
| `PS-007` | Open | Scripts | Jeff Skone | Related to | `PS-003`, `PS-006` | Create script to verify files against a SHA-256 sidecar and compare file identity | Evaluate, plan, and develop the proposed comparison utility further before treating the current draft as a final repository component. Starting artifacts are `Compare-SHA256.ps1` and `Compare-SHA256-Addendum.md`; both should be included when this work is implemented, with the addendum retained as design/usage input and adapted into the component's authoritative README or supporting documentation as appropriate. The script's current goal is to read exactly one `*.sha256` sidecar in the current folder, calculate SHA-256 for one or more other files even when their filenames differ, report whether all/some/none match the expected hash, indicate whether the candidate files share the same calculated SHA-256, show the actual SHA-256 for mismatches, and provide a clear success result when every file matches. Preserve reasonably detailed source comments so the logic and flow are easy to follow, generic non-sensitive examples, Windows PowerShell 5.1 compatibility where practical, direct CLI copy/paste usability as well as `.ps1` execution, the default-off optional detail flag, and automatic exclusion of the running script when stored beside the files being checked. Further evaluate terminology and documentation around SHA-256, hash, checksum, and sidecar; whether matching hashes are sufficient for the intended meaning of file identity or whether optional literal byte-by-byte comparison is warranted; formal parameters such as `-Path` and `-ShowDetails`; native `-Verbose`; explicit exit codes; multi-line checksum manifests; recursion; alternate algorithms; structured output; include/exclude patterns; tests; and final component naming. Because this overlaps the broader sidecar validation work in `PS-006`, explicitly decide during planning whether it should remain a distinct focused component, be incorporated into `Test-FolderSha256.ps1`, or share reusable logic/documentation with that work. In coordination with `PS-003`, also reconsider whether checksum-related utilities justify a category/subfolder. Candidate structures include a simple category such as `scripts/checksums/<component>/` or `scripts/file-integrity/<component>/`, and—only if future inventory justifies the added depth—a nested category/subcategory such as `scripts/file-integrity/checksums/<component>/`. Treat `checksum`, `checksums`, `file-integrity`, `hashing`, or another durable term as candidates rather than a settled taxonomy; define category scope before adoption and keep the hierarchy as shallow as practical. |
| `PS-008` | Open | Scripts | Jeff Skone | Related to | `PS-003` | Create script to hold a file open for lock-contention testing | Evaluate, plan, and develop a small test utility that intentionally opens a user-selected file and keeps the file handle open until the user explicitly provides input, allowing other commands, scripts, applications, or test cases to exercise file-in-use and lock-contention behavior. The preferred implementation should explicitly control file sharing rather than relying on a normal open operation alone; evaluate using a .NET `FileStream` with `FileShare.None` so the test can create an exclusive lock where supported, then pause with a clear prompt such as Enter or any key and reliably close/dispose the handle after input. Use `try`/`finally` or equivalent cleanup so the handle is released on normal completion and as reliably as practical on errors or interruption. Evaluate a PowerShell-approved, intention-revealing filename such as `Start-FileLockTest.ps1`, final parameter design (at minimum a file path), read-only versus read/write access requirements, whether alternate sharing modes are useful for testing, behavior for nonexistent paths or files already locked by another process, console messaging that clearly states which file is held and how to release it, Windows PowerShell 5.1 and PowerShell 7+ compatibility, exit-code behavior, comment-based help, examples, and focused tests. Keep the utility safe and explicit: it should not modify file contents merely to hold the lock unless a selected mode requires write access. Treat this as a testing/development utility rather than a general-purpose production locking mechanism. In coordination with `PS-003`, consider whether utilities of this type eventually justify a category such as `testing`, `file-system`, or another durable taxonomy, while avoiding a new category solely for one script. |